# 关键产出标题质量改进设计

## 问题描述

当前AI生成的关键产出标题过于事务性，例如"文档已全部生成"、"关键设计决策明确"、"交付范围清晰"、"原型8项约束已覆盖"，这些标题没有提供足够的信息帮助用户决策。

用户希望标题能够：
1. 一目了然的内容提要
2. 直接反映产出的核心内容
3. 让用户快速理解产出是什么

## 当前实现

在`server/prompts/index.ts`中的`buildSummarizationPrompt`函数，提示词中对key_points的title描述为"要点概要，不超过50字"，这导致AI生成过于事务性的标题。

## 改进方案

修改提示词，明确要求标题要具体、描述性，而不是事务性总结。给出好的标题例子和坏的标题例子，让AI理解什么算好标题。

## 改进后的提示词

```typescript
export function buildSummarizationPrompt(summary: string): string {
  return `你是一个任务总结专家。请严格基于下面的 Agent 工作摘要，生成结构化总结。

要求：
1. 忠于原文，不添加原文中没有的内容，不自由发挥
2. 仅输出 JSON，不要有任何额外说明文字

输出 JSON schema：
{
  "brief": "核心总结，不超过200字",
  "key_points": [
    { "title": "具体描述产出的核心内容，不超过50字", "summary": "要点内容，不超过200字" }
  ],
  "todos": [
    {
      "task": "需要用户决策或讨论的问题",
      "type": "choice" | "fill",
      "multiSelect": true/false,
      "choices": [{ "option": "选项名", "description": "选项描述" }],
      "placeholder": "填空题占位文本"
    }
  ]
}

注意：
- key_points 数量不限，提取核心要点
- todos 为待决策事项，字段必填，且type字段必填
- 若无待决策问题，则type为choice，且仅包含一个选项:需求已明确，进入下一阶段
- 必须包含type字段，并且仅支持choice 和fill 两种类型
- type=choice 时 choices 必填，type=fill 时 choices 可为空数组、placeholder 必填
- multiSelect 仅 type=choice 时有效，默认 false
- type=choice 时，必须包含一个选项:需求已明确，进入下一阶段
- brief 使用中文

**标题要求（重要）**：
- 标题应该具体描述产出的核心内容，让用户一目了然
- 标题应该反映"产出了什么"或"做了什么"，而不是"总结了什么"
- 避免使用事务性词汇如"已生成"、"已明确"、"已清晰"、"已覆盖"等
- 好标题例子：基于微服务架构的服务设计、用户点击-弹窗-填写工作流、WorkTree分支回退机制、采用DDD领域驱动设计、API接口规范文档、前端组件库设计
- 坏标题例子：文档已全部生成、关键设计决策明确、交付范围清晰、原型8项约束已覆盖、需求分析完成、技术方案确定

以下是 Agent 工作摘要：
---
${summary}
---`;
}
```

## 改进点说明

1. **明确标题要求**：在提示词中增加"标题要求"部分，明确说明标题应该具体、描述性
2. **给出好标题例子**：提供具体的例子，让AI理解什么算好标题
3. **给出坏标题例子**：提供反例，让AI避免生成事务性标题
4. **强调用户视角**：强调标题应该让用户一目了然，快速理解产出内容

## 预期效果

改进后，AI生成的关键产出标题将更加具体、描述性，例如：
- "基于微服务架构的服务设计" 而不是 "架构设计已完成"
- "用户点击-弹窗-填写工作流" 而不是 "交互流程已明确"
- "WorkTree分支回退机制" 而不是 "版本控制机制已设计"
- "采用DDD领域驱动设计" 而不是 "设计模式已确定"

## 实现步骤

1. 修改`server/prompts/index.ts`中的`buildSummarizationPrompt`函数
2. 测试改进后的提示词，验证标题质量
3. 部署到生产环境

## 验证方法

1. 运行AI总结任务，检查生成的标题是否符合预期
2. 收集用户反馈，确认标题质量改进
3. 监控标题质量，确保持续改进