# task-1780387650243

本项目由 AI Agent 驱动生成。

## 业务意图
为满足项目重构后的集成需求，需要提供可通过接口调用的方式自动生成 Swagger 差异报告的能力。该能力应接收两个 Swagger 文档作为输入参数，并返回结构化的差异结果，以便业务系统能够灵活嵌入 API 变更检测、版本对比或兼容性校验等场景。

## 规范
- 代码使用 TypeScript + React
- 样式使用 CSS（保持与平台 UI 风格一致）
- 数据先 mock，存在 localStorage